import { API_URL } from 'react-native-dotenv';

const CHECKINS_ADD_URL = `${API_URL}checkins/add`;
const CHECKINS_URL = `${API_URL}checkins`;
const PLACES_URL = `${API_URL}places`;
const SAKES_URL = `${API_URL}sakes`;
const USERS_URL = `${API_URL}users`;

export {
  CHECKINS_ADD_URL,
  CHECKINS_URL,
  PLACES_URL,
  SAKES_URL,
  USERS_URL
}
